name: testing_app
description: "A new Flutter project."
publish_to: 'none' # This is so that the project won’t be published to pub.dev
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  go_router: ^13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
